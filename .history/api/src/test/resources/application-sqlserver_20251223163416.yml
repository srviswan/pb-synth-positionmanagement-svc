# Test configuration for MS SQL Server
spring:
  datasource:
    type: sqlserver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration/sqlserver
