spring:
  application:
    name: position-management-service
  
  # SQL Server Configuration (set via environment variables or defaults)
  datasource:
    url: ${DB_URL:jdbc:sqlserver://localhost:1433;databaseName=equity_swap_db;encrypt=true;trustServerCertificate=true}
    username: ${DB_USERNAME:SA}
    password: ${DB_PASSWORD:Test@123456}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    hikari:
      maximum-pool-size: 50
      minimum-idle: 10
      connection-timeout: 20000
      leak-detection-threshold: 60000
  
  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc.batch_versioned_data: true
        connection.provider_disables_autocommit: true
        generate_statistics: false
    show-sql: false
  
  flyway:
    enabled: true
    locations: classpath:db/migration/sqlserver
    baseline-on-migrate: true
  
  # Kafka Configuration
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: ${KAFKA_CONSUMER_GROUP_ID:position-management-service}
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
        enable.auto.commit: false
  
  # Solace Configuration (only used when app.messaging.provider=solace)
  # Note: Solace Spring Boot Starter uses solace.java.* properties
  solace:
    java:
      host: ${SOLACE_HOST:tcp://localhost:55556}
      msg-vpn: ${SOLACE_VPN:default}
      client-username: ${SOLACE_USERNAME:admin}
      client-password: ${SOLACE_PASSWORD:admin}

  # Redis Configuration
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      lettuce:
        pool:
          max-active: 50
          max-idle: 20
          min-idle: 10

# Application-specific configuration
app:
  kafka:
    topics:
      backdated-trades: ${KAFKA_TOPIC_BACKDATED_TRADES:backdated-trades}
      trade-applied-events: ${KAFKA_TOPIC_TRADE_APPLIED:trade-applied-events}
      provisional-trade-events: ${KAFKA_TOPIC_PROVISIONAL:provisional-trade-events}
      historical-position-corrected-events: ${KAFKA_TOPIC_CORRECTED:historical-position-corrected-events}
      dlq: ${KAFKA_TOPIC_DLQ:trade-events-dlq}
      error-queue: ${KAFKA_TOPIC_ERROR_QUEUE:trade-events-errors}
      regulatory-submissions: ${KAFKA_TOPIC_REGULATORY:regulatory-submissions}
  messaging:
    provider: ${MESSAGING_PROVIDER:kafka} # kafka or solace
  solace:
    host: ${SOLACE_HOST:localhost}
    port: ${SOLACE_PORT:55555}
    vpn: ${SOLACE_VPN:default}
    username: ${SOLACE_USERNAME:default}
    password: ${SOLACE_PASSWORD:}
    client-name: ${SOLACE_CLIENT_NAME:position-management-service}
    default-destination-type: ${SOLACE_DESTINATION_TYPE:topic} # topic or queue
    topics:
      backdated-trades: ${SOLACE_TOPIC_BACKDATED_TRADES:backdated-trades}
      trade-applied-events: ${SOLACE_TOPIC_TRADE_APPLIED:trade-applied-events}
      provisional-trade-events: ${SOLACE_TOPIC_PROVISIONAL:provisional-trade-events}
      historical-position-corrected-events: ${SOLACE_TOPIC_CORRECTED:historical-position-corrected-events}
      dlq: ${SOLACE_TOPIC_DLQ:trade-events-dlq}
      error-queue: ${SOLACE_TOPIC_ERROR_QUEUE:trade-events-errors}
      regulatory-submissions: ${SOLACE_TOPIC_REGULATORY:regulatory-submissions}
  cache:
    provider: ${CACHE_PROVIDER:redis} # redis or memory
  
  # IAM Service Configuration
  iam:
    service:
      url: ${IAM_SERVICE_URL:http://localhost:8081}
    cache:
      enabled: ${IAM_CACHE_ENABLED:true}
      ttl:
        minutes: ${IAM_CACHE_TTL_MINUTES:5}
  
  # Authorization Configuration
  authorization:
    enabled: ${AUTHORIZATION_ENABLED:true}
    allow-anonymous: ${AUTHORIZATION_ALLOW_ANONYMOUS:false} # Set to true for development

# Server Configuration
server:
  port: 8080

# Actuator Configuration
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}

# Resilience4j Configuration
resilience4j:
  circuitbreaker:
    instances:
      contractService:
        registerHealthIndicator: true
        failureRateThreshold: 50
        waitDurationInOpenState: 60s
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
      hotpathDatabase:
        registerHealthIndicator: true
        failureRateThreshold: 50
        waitDurationInOpenState: 30s
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
      coldpathDatabase:
        registerHealthIndicator: true
        failureRateThreshold: 50
        waitDurationInOpenState: 60s
        slidingWindowSize: 20
        minimumNumberOfCalls: 10
      kafkaProducer:
        registerHealthIndicator: true
        failureRateThreshold: 50
        waitDurationInOpenState: 30s
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
  retry:
    instances:
      hotpath:
        maxAttempts: 3
        waitDuration: 50ms
      coldpath:
        maxAttempts: 5
        waitDuration: 200ms
  timelimiter:
    instances:
      hotpathDatabase:
        timeoutDuration: 50ms
      contractService:
        timeoutDuration: 40ms
      coldpath:
        timeoutDuration: 5m
